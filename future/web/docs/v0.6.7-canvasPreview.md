Searched text for `panel.remove =` (`**/future/web/ui/dev-panel/*`), 1 result

I'll read dev-panel.js around the setupUI function and the panel.remove override so I can insert the preview-canvas code in the right place and add cleanup. Expected outcome: identify insertion points and prepare a patch. 

Read [](file:///workspaces/acoustsee/future/web/ui/dev-panel/dev-panel.js#520-520), lines 520 to 676

Created 1 todo

I'll apply a patch to dev-panel.js that removes the <video> preview element from the HTML template and inserts the preview-container + wiring inside `setupUI`. I'll also add cleanup to the panel.remove override. Expected outcome: dev panel no longer creates a video element and uses preview canvas instead. I'll edit the file. 

Using "Apply Patch"


```

```
Now I will insert the wiring code (startPreview/stopPreview and toggle wiring) into `setupUI` after the video preview wiring area — there's an existing "Cost-Effective Video Preview Wiring" block which earlier wired a real <video> preview; I'll replace that block with code that binds to the processing canvas and uses setInterval to draw it to the preview canvas. I'll find the existing preview wiring block lines and replace them. 

Searched text for `Cost-Effective Video Preview Wiring` (`**/future/web/ui/dev-panel/dev-panel.js`), 2 results

Read [](file:///workspaces/acoustsee/future/web/ui/dev-panel/dev-panel.js#468-468), lines 468 to 520

I'll replace the existing preview wiring block with a new implementation that selects the processing canvas and draws it into `#devpanel-preview-canvas` at a low frame rate when the toggle is checked. I'll prepare and apply the patch. 

Using "Apply Patch"


```

```
Now I need to extend the panel.remove override to also stop and cleanup the preview interval/ResizeObserver if present. Previously originalRemove override cleaned only state inspector. I'll replace that block to include preview cleanup. I'll apply a patch. 

Using "Apply Patch"


```

```


Checked [](file:///workspaces/acoustsee/future/web/ui/dev-panel/dev-panel.js), no problems found

Progress update: I edited dev-panel.js to remove the <video> preview and added a low-FPS processing canvas preview plus cleanup. Next I'll run a quick grep to ensure no left-over references to `devpanel-video-preview` remain and that `devpanel-preview-canvas` is present. 

Searched text for `devpanel-video-preview|devpanel-preview-canvas|devpanel-preview-toggle|__previewInterval|__previewRO`, 52 results

Checked [](file:///workspaces/acoustsee/future/web/ui/dev-panel/dev-panel.js), [](file:///workspaces/acoustsee/future/web/ui/dev-panel/state-inspector.js), no problems found

Summary of changes I'm about to commit
- Replaced the dev-panel <video> preview with a low-overhead preview canvas and a toggle.
- Added wiring in `setupUI` to sample the processing canvas (e.g., `DOM.frameCanvas`) at low FPS (default 4 FPS) using a safe setInterval and ResizeObserver.
- Ensured cleanup in `panel.remove` disposes the preview interval/observer and the StateInspector.

What I changed
- Edited dev-panel.js:
  - Removed the inline <video id="devpanel-video-preview"> element from the panel template.
  - Inserted a `<canvas id="devpanel-preview-canvas">` and a checkbox toggle `#devpanel-preview-toggle` into the panel HTML.
  - Replaced the old video-stream wiring with a processing-canvas-to-preview wiring block:
    - pickProcessingCanvas() finds `DOM.frameCanvas` or sensible fallbacks.
    - startPreview/stopPreview manage a ResizeObserver and a setInterval that draws the processing canvas into the preview canvas at low FPS.
    - The preview only starts when the toggle is checked; it does not create new decoders or media streams.
  - Extended `panel.remove` cleanup to stop the preview interval and disconnect the ResizeObserver.

Why this meets your goals
- Single decoder: we no longer create another video element bound to a MediaStream. The preview simply copies from the existing processing canvas (or a DOM canvas), so no extra camera decoding or stream cloning is involved.
- Low battery/CPU: copying a canvas at 2–5 FPS is low overhead; the preview defaults to 4 FPS and clamps a maximum of 10 FPS. The inspector and UI remain throttled as before.
- Single source of truth: video dimensions are still provided by media-commands.js into `engine.state.videoSize` and are rendered by `StateInspector`.
- Safe cleanup: preview interval and ResizeObserver are cleared when the panel is removed.

Progress update
- Files edited: 2
  - dev-panel.js — replaced video preview with canvas + wiring.
  - state-inspector.js — previously updated to render videoSize and support filtering.
- Verified: no syntax errors reported for the edited files.

Next steps (choose one)
- I can remove the now-unused `.dev-panel` CSS rules for `#devpanel-video-preview` from dev-panel.css to keep styles tidy.
- Add an option to let the Dev Panel choose target FPS for the preview (slider or select) — low risk and useful.
- Add a tiny smoke test that mounts the dev panel in a headless DOM and asserts that toggling preview starts/stops the interval (requires test setup).

If you want me to proceed with any of the next steps, tell me which one and I'll add it and run quick checks.

